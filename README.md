# NeCNN

## Installing
To use the package install it with `pip install .

## Running examples
To run the examples the best way is to set up a pipenv with `pipenv install`
Afterwards you are able to run one of the example files.